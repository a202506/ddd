package com.buzzingmountain.dingclock

import org.junit.Assert.assertEquals
import org.junit.Test

class SanityTest {
    @Test
    fun `phase 0 build sanity`() {
        assertEquals(4, 2 + 2)
    }
}
